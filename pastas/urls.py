# pastas/urls.py
from django.urls import path
from . import views

app_name = 'pastas'

urlpatterns = [
    # Atualmente não temos nenhuma URL específica para este app,
    # mas o arquivo precisa existir para a inclusão funcionar.
]